---
import Layout from "../../layouts/Layout.astro";
import NavbarPage from "../../components/ui/navbar-page"
import Footer from "../../components/page/footer"
import AnnouncementList from "../../components/page/announcement";
import { getPublishedAnnouncements } from "../../db/queries/announcement";

const announcements = await getPublishedAnnouncements()
---

<Layout title="Pengumuman | PPDB Online SMP IT DARUSSALAM LUWUK" description="Pengumuman">
  <NavbarPage client:only="react" />
  <AnnouncementList client:only="react" announcements={announcements}/>
  <Footer client:only="react" />
</Layout>